/**
 * Package root entry — re-exports protocol + verifiers for
 * `import … from "@pymthouse/clearinghouse-identity-webhook"`.
 * Prefer subpath imports (`/protocol`, `/verifiers`) in new code.
 */
export * from "./protocol.js";
export * from "./verifiers.js";
